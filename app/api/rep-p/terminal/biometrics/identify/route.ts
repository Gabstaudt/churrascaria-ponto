import { z } from "zod";
import { authenticateTerminal } from "@/services/terminal-auth.service";
import { consumeRateLimit, rateLimitResponse } from "@/services/rate-limit.service";
import { BiometricDecisionError, identifyForPoint } from "@/modules/biometrics/services/biometric.service";
const schema = z.object({ locationValidationId: z.uuid(), imageBase64: z.string().max(3_500_000) }).strict(); export const runtime = "nodejs";
export async function POST(request: Request) { const collector = await authenticateTerminal(); if (!collector) return Response.json({ error: "Terminal não autorizado." }, { status: 401 }); const limited = await consumeRateLimit(request, "biometric-identify", 5, 180, collector.id); if (!limited.allowed) return rateLimitResponse(limited); const parsed = schema.safeParse(await request.json().catch(() => null)); if (!parsed.success) return Response.json({ error: "Captura inválida." }, { status: 400 }); try { const image = Buffer.from(parsed.data.imageBase64.replace(/^data:image\/jpeg;base64,/, ""), "base64"); const result = await identifyForPoint({ collectorId: collector.id, locationValidationId: parsed.data.locationValidationId, image }); return Response.json(result, { status: 201, headers: { "Cache-Control": "no-store" } }); } catch (error) { if (error instanceof BiometricDecisionError) return Response.json({ error: error.message, code: error.code }, { status: 422 }); return Response.json({ error: "Serviço biométrico temporariamente indisponível.", code: "PROVIDER_UNAVAILABLE" }, { status: 503 }); } }
