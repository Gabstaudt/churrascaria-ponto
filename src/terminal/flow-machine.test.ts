import { describe, expect, it } from "vitest";
import { initialTerminalFlowState, terminalFlowReducer } from "./flow-machine";
describe("terminalFlowReducer", () => { it("valida localização antes da identificação", () => { const requesting = terminalFlowReducer(initialTerminalFlowState, { type: "START" }); const checking = terminalFlowReducer(requesting, { type: "LOCATION_CAPTURED" }); const identifying = terminalFlowReducer(checking, { type: "LOCATION_VALID", validationId: "location-1" }); const validating = terminalFlowReducer(identifying, { type: "IDENTIFIED", employee: { id: "1", displayName: "João" } }); const ready = terminalFlowReducer(validating, { type: "VALIDATED" }); expect(ready).toMatchObject({ status: "READY", locationValidationId: "location-1", employee: { id: "1" } }); }); it("rejeita localização sem avançar", () => { const requesting = terminalFlowReducer(initialTerminalFlowState, { type: "START" }); expect(terminalFlowReducer(requesting, { type: "LOCATION_REJECTED", message: "Negada" })).toEqual({ status: "LOCATION_REJECTED", error: "Negada" }); }); it("limpa identidade e localização ao reiniciar", () => expect(terminalFlowReducer({ status: "SUCCESS", locationValidationId: "location-1", employee: { id: "1", displayName: "João" }, result: { eventLabel: "Entrada", recordedAt: "now", nsr: "1" } }, { type: "RESET" })).toEqual({ status: "IDLE" })); });
