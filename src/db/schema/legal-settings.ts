import { boolean, pgTable, timestamp, uniqueIndex, uuid, varchar } from "drizzle-orm/pg-core";
import { users } from "./users";

export const legalSettings = pgTable("legal_settings", { id: uuid("id").defaultRandom().primaryKey(), installationKey: varchar("installation_key", { length: 30 }).notNull().default("PRIMARY"), employerName: varchar("employer_name", { length: 150 }).notNull(), employerIdType: varchar("employer_id_type", { length: 3 }).notNull(), employerId: varchar("employer_id", { length: 14 }).notNull(), caepf: varchar("caepf", { length: 14 }), cno: varchar("cno", { length: 12 }), street: varchar("street", { length: 200 }).notNull(), district: varchar("district", { length: 100 }).notNull(), postalCode: varchar("postal_code", { length: 8 }).notNull(), city: varchar("city", { length: 100 }).notNull(), state: varchar("state", { length: 2 }).notNull(), legalRepresentative: varchar("legal_representative", { length: 150 }).notNull(), employerEmail: varchar("employer_email", { length: 255 }).notNull(), ptrpName: varchar("ptrp_name", { length: 150 }).notNull(), ptrpVersion: varchar("ptrp_version", { length: 8 }).notNull(), developerIdType: varchar("developer_id_type", { length: 3 }).notNull(), developerId: varchar("developer_id", { length: 14 }).notNull(), developerName: varchar("developer_name", { length: 150 }).notNull(), developerEmail: varchar("developer_email", { length: 50 }).notNull(), officialExportsEnabled: boolean("official_exports_enabled").notNull().default(false), complianceReviewedAt: timestamp("compliance_reviewed_at", { withTimezone: true }), complianceReviewedBy: uuid("compliance_reviewed_by").references(() => users.id, { onDelete: "restrict" }), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (table) => [uniqueIndex("legal_settings_installation_unique").on(table.installationKey)]);
