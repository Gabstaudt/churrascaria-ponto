import { describe, expect, it } from "vitest";
import { haversineDistanceMeters, validateGeofence } from "./geofence-core";
const now = new Date("2026-08-14T12:00:00Z"); const policy = { latitude: -1.356, longitude: -48.372, enabled: true, radiusMeters: 60, maximumAccuracyMeters: 30, maxAgeSeconds: 30 };
describe("GeofenceService", () => { it("retorna zero para coordenadas iguais", () => expect(haversineDistanceMeters(policy, policy)).toBe(0)); it("usa limite inclusivo", () => expect(validateGeofence({ latitude: policy.latitude + 60 / 111_195, longitude: policy.longitude, accuracyMeters: 5, capturedAt: now, source: "DEVICE_GEOLOCATION" }, policy, now).status).toBe("VALID")); it("rejeita baixa precisão e localização expirada", () => { expect(validateGeofence({ ...policy, accuracyMeters: 31, capturedAt: now, source: "DEVICE_GEOLOCATION" }, policy, now).status).toBe("LOW_ACCURACY"); expect(validateGeofence({ ...policy, accuracyMeters: 5, capturedAt: new Date(now.getTime() - 31_000), source: "DEVICE_GEOLOCATION" }, policy, now).status).toBe("EXPIRED"); }); it("sinaliza coordenadas absurdas", () => expect(validateGeofence({ latitude: 91, longitude: -181, accuracyMeters: 0, capturedAt: now, source: "DEVICE_GEOLOCATION" }, policy, now).status).toBe("SUSPICIOUS")); });
