import { describe, expect, it } from "vitest";
import { biometricValidationUsable, decideFacialMatch } from "./biometric-policy";
describe("política biométrica", () => { it("não aceita o melhor candidato abaixo do threshold", () => expect(decideFacialMatch({ bestScore: .79, secondBestScore: .2, minimumSimilarityThreshold: .8, minimumScoreGap: .08 })).toBe("NO_MATCH")); it("rejeita candidatos ambíguos", () => expect(decideFacialMatch({ bestScore: .91, secondBestScore: .89, minimumSimilarityThreshold: .8, minimumScoreGap: .08 })).toBe("AMBIGUOUS")); it("aprova somente score e separação suficientes", () => expect(decideFacialMatch({ bestScore: .91, secondBestScore: .7, minimumSimilarityThreshold: .8, minimumScoreGap: .08 })).toBe("MATCH")); it("impede replay", () => { const now = new Date(); expect(biometricValidationUsable({ status: "APPROVED", expiresAt: new Date(now.getTime() + 1), usedAt: null }, now)).toBe(true); expect(biometricValidationUsable({ status: "APPROVED", expiresAt: new Date(now.getTime() + 1), usedAt: now }, now)).toBe(false); }); });
