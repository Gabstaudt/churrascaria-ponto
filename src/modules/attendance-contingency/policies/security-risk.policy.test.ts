import { describe, expect, it } from "vitest";
import { clockRisk, suspectedCollectorClone } from "./security-risk.policy";
describe("políticas antifraude do terminal", () => { it("detecta credencial usada por instâncias simultâneas", () => { const now = new Date("2026-08-15T12:00:00Z"); expect(suspectedCollectorClone({ previousInstanceId: "a", currentInstanceId: "b", previousSeenAt: new Date(now.getTime() - 30_000), now })).toBe(true); expect(suspectedCollectorClone({ previousInstanceId: "a", currentInstanceId: "a", previousSeenAt: now, now })).toBe(false); }); it("mantém timezone oficial e sinaliza relógio divergente", () => { const result = clockRisk({ deviceCapturedAt: new Date("2026-08-15T12:00:00Z"), clockOffsetMs: 3 * 60_000, now: new Date("2026-08-15T12:03:00Z"), officialTimeZone: "America/Belem", deviceTimeZone: "Europe/London" }); expect(result.flags).toEqual(expect.arrayContaining(["CLOCK_DRIFT_HIGH", "DEVICE_TIMEZONE_MISMATCH"])); }); });
