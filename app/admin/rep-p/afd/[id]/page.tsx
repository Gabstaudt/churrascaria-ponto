import { AlertTriangle, ArrowLeft, Download, FileArchive, RefreshCw } from "lucide-react";
import Link from "next/link";
import { notFound } from "next/navigation";
import { retryAfdGenerationAction, signAfdAction } from "@/actions/afd";
import { getAfdGeneration } from "@/modules/afd/services/afd-generation.service";
import { requireAfdPermission } from "@/modules/afd/services/afd-permission.service";

export default async function AfdDetailPage({ params, searchParams }: PageProps<"/admin/rep-p/afd/[id]">) { await requireAfdPermission("AFD_VIEW_HISTORY"); const { id } = await params; const query = await searchParams; const row = await getAfdGeneration(id); if (!row) notFound(); const item = row.generation; return <main className="page-content afd-detail-page"><Link className="back-link" href="/admin/rep-p/afd"><ArrowLeft size={16} /> Voltar ao histórico</Link><header className="page-heading"><div><p className="eyebrow">AFD · detalhes da geração</p><h1>{item.fileName ?? "Geração não concluída"}</h1><p>{row.registrarName} · {row.establishmentName}</p></div><span className={`portal-status ${item.status === "FAILED" ? "rejected" : item.status === "SIGNED" ? "approved" : "pending"}`}>{item.status}</span></header>{query.error ? <p className="portal-feedback error">A assinatura não pôde ser concluída. Verifique o certificado e tente novamente.</p> : null}<section className="afd-detail-grid"><article><span>Período</span><strong>{item.startDate} a {item.endDate}</strong></article><article><span>NSR inicial</span><strong>{item.firstNsr ?? "—"}</strong></article><article><span>NSR final</span><strong>{item.lastNsr ?? "—"}</strong></article><article><span>Registros</span><strong>{item.recordCount}</strong></article><article><span>Leiaute</span><strong>{item.layoutVersion}</strong></article><article><span>Assinatura CAdES</span><strong>{item.signatureStatus}</strong></article></section><section className="content-panel afd-hash-panel"><header><div><h2>Integridade</h2><p>Hashes SHA-256 do arquivo e da assinatura destacada.</p></div></header><code>{item.fileHash ?? "Arquivo não materializado."}</code>{item.signatureHash ? <code>{item.signatureHash}</code> : null}</section><section className="content-panel"><header><div><h2>Erros e avisos</h2><p>Problemas devem ser corrigidos na fonte; linhas do AFD nunca são editadas.</p></div></header><div className="afd-issues">{item.validationIssues.map((issue, index) => <article key={`${issue.code}-${index}`}><AlertTriangle /><div><strong>{issue.code}</strong><p>{issue.message}{issue.nsr ? ` · NSR ${issue.nsr}` : ""}</p></div></article>)}{!item.validationIssues.length ? <p>Nenhuma inconsistência encontrada.</p> : null}</div></section><div className="form-actions">{item.status === "FAILED" ? <form action={retryAfdGenerationAction.bind(null, item.id)}><button className="primary-button"><RefreshCw size={16} /> Nova tentativa</button></form> : null}{item.status === "GENERATED" || item.signatureStatus === "FAILED" ? <form action={signAfdAction.bind(null, item.id)}><button className="primary-button"><FileArchive size={16} /> Assinar com CAdES</button></form> : null}{item.status === "SIGNED" && item.signatureStatus === "SIGNED" ? <><Link className="primary-button" href={`/api/rep-p/afd/${item.id}/download`}><Download size={16} /> Baixar AFD</Link><Link className="secondary-button" href={`/api/rep-p/afd/${item.id}/signature`}><Download size={16} /> Baixar .p7s</Link></> : <span className="afd-signature-notice"><FileArchive size={17} /> O download oficial exige assinatura CAdES validada.</span>}</div></main>; }
