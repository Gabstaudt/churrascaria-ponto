"use server";
import { redirect } from "next/navigation";
import { requireAdmin } from "@/auth/session";
import { enrollEmployeeBiometric, revokeEmployeeBiometric } from "@/modules/biometrics/services/biometric.service";
import { db } from "@/db";
import { recordAudit } from "@/services/audit.service";
export async function enrollBiometricAction(employeeId: string, formData: FormData) { const session = await requireAdmin(); const image = String(formData.get("image") ?? ""); if (!image.startsWith("data:image/jpeg;base64,") || image.length > 3_500_000 || formData.get("acknowledged") !== "on") redirect(`/admin/funcionarios/${employeeId}/biometria?error=invalid`); try { const profile = await enrollEmployeeBiometric({ employeeId, image: Buffer.from(image.split(",")[1], "base64"), performedBy: session.user.id, privacyNoticeVersion: "1.0", policyVersion: "1.0" }); await recordAudit(db, { action: "BIOMETRIC_ENROLLED", entity: "EMPLOYEE_BIOMETRIC_PROFILE", entityId: profile.id, performedBy: session.user.id, after: { employeeId, status: profile.status, provider: profile.provider, algorithmVersion: profile.algorithmVersion } }); } catch { redirect(`/admin/funcionarios/${employeeId}/biometria?error=capture`); } redirect(`/admin/funcionarios/${employeeId}/biometria?saved=1`); }
export async function revokeBiometricAction(employeeId: string) { const session = await requireAdmin(); const profile = await revokeEmployeeBiometric(employeeId); if (profile) await recordAudit(db, { action: "BIOMETRIC_REVOKED", entity: "EMPLOYEE_BIOMETRIC_PROFILE", entityId: profile.id, performedBy: session.user.id, before: { employeeId, status: "ACTIVE" }, after: { employeeId, status: "REVOKED" } }); redirect(`/admin/funcionarios/${employeeId}/biometria?revoked=1`); }
