import type { IdentifiedEmployee } from "./identification";

export type TerminalFlowStatus = "IDLE" | "REQUESTING_LOCATION" | "CHECKING_LOCATION" | "LOCATION_REJECTED" | "IDENTIFYING" | "VALIDATING" | "READY" | "REGISTERING" | "SUCCESS" | "ERROR";
export type TerminalFlowState = { status: TerminalFlowStatus; locationValidationId?: string; employee?: IdentifiedEmployee; result?: { eventLabel: string; recordedAt: string; nsr: string }; error?: string };
export type TerminalFlowEvent = { type: "START" } | { type: "LOCATION_CAPTURED" } | { type: "LOCATION_VALID"; validationId: string } | { type: "LOCATION_REJECTED"; message: string } | { type: "IDENTIFIED"; employee: IdentifiedEmployee } | { type: "VALIDATED" } | { type: "REGISTER" } | { type: "SUCCEEDED"; result: NonNullable<TerminalFlowState["result"]> } | { type: "FAILED"; message: string } | { type: "RESET" };
export const initialTerminalFlowState: TerminalFlowState = { status: "IDLE" };

export function terminalFlowReducer(state: TerminalFlowState, event: TerminalFlowEvent): TerminalFlowState { if (event.type === "RESET") return initialTerminalFlowState; switch (state.status) { case "IDLE": return event.type === "START" ? { status: "REQUESTING_LOCATION" } : state; case "REQUESTING_LOCATION": return event.type === "LOCATION_CAPTURED" ? { status: "CHECKING_LOCATION" } : event.type === "LOCATION_REJECTED" ? { status: "LOCATION_REJECTED", error: event.message } : state; case "CHECKING_LOCATION": return event.type === "LOCATION_VALID" ? { status: "IDENTIFYING", locationValidationId: event.validationId } : event.type === "LOCATION_REJECTED" ? { status: "LOCATION_REJECTED", error: event.message } : state; case "IDENTIFYING": return event.type === "IDENTIFIED" ? { status: "VALIDATING", employee: event.employee, locationValidationId: state.locationValidationId } : event.type === "FAILED" ? { status: "ERROR", error: event.message } : state; case "VALIDATING": return event.type === "VALIDATED" ? { ...state, status: "READY" } : event.type === "FAILED" ? { status: "ERROR", error: event.message } : state; case "READY": return event.type === "REGISTER" ? { ...state, status: "REGISTERING" } : state; case "REGISTERING": return event.type === "SUCCEEDED" ? { ...state, status: "SUCCESS", result: event.result } : event.type === "FAILED" ? { status: "ERROR", error: event.message } : state; default: return state; } }
