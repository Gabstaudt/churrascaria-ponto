import { beforeEach, describe, expect, it } from "vitest";
import { decryptBiometricTemplate, encryptBiometricTemplate } from "./biometric-encryption.service";
describe("BiometricEncryptionService", () => { beforeEach(() => { process.env.BIOMETRIC_ENCRYPTION_KEY = Buffer.alloc(32, 7).toString("base64"); }); it("protege template com cifra autenticada", () => { const result = encryptBiometricTemplate([0.1, 0.2, -0.3]); expect(result.encrypted).not.toContain("0.1"); expect(decryptBiometricTemplate(result.encrypted)).toEqual([0.1, 0.2, -0.3]); }); it("rejeita conteúdo adulterado", () => { const parts = encryptBiometricTemplate([1]).encrypted.split("."); parts[3] = `${parts[3][0] === "A" ? "B" : "A"}${parts[3].slice(1)}`; expect(() => decryptBiometricTemplate(parts.join("."))).toThrow(); }); });
